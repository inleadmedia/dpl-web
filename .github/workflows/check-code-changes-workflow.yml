---
# Check if specific code is present, if certain files have been changed.
# This is especially useful in the CMS, as certain config changes requires us
# to also run an update hook.

on:
  workflow_call:
    inputs:
      trigger-path:
        required: true
        type: string
      target-code:
        required: true
        type: string
jobs:
  check:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: cms
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          ref: ${{ github.head_ref }}
      - name: If trigger paths have changed, check if target code is present
        shell: bash
        run: |
          if git diff --name-only origin/${{ github.base_ref }}...HEAD | grep -E '${{ inputs.trigger-path }}'; then
            echo "${{ inputs.trigger-path }} config was changed, checking for hook..."
            if ! git diff origin/${{ github.base_ref }}...HEAD -- web/ | grep -E '${{ inputs.target-code }}'; then
              echo "::error file=${{ inputs.trigger-path }}::${{ inputs.trigger-path }} config changed but '${{ inputs.target-code }}' hook not found in changes."
              exit 1
            fi
          else
            echo "No relevant changes detected."
          fi
