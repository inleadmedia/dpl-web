{{ include('@eonext_kultur_main/content-slider-articles.html.twig', {
  label: label,
  logo_url: logo_url,
  items: items,
  articles_overview_url: articles_overview_url,
  full_width: full_width|default(true),
  items_per_view: items_per_view|default(2),
}, false) }}
