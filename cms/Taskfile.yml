# This is a go-task file for various developer tasks
# e.g. building docker images and setting up local development.
# You can read about the Task files here: https://taskfile.dev.
version: "3"

dotenv: [".task.env"]

interval: 500ms

env:
  # The containers must run as the host user or the repo fills up with files
  # you cannot edit. From the host, so a new checkout needs no setup. See
  # docker-compose.yml for why the group is not set the same way.
  UID:
    sh: id -u

  # Everything this Taskfile runs is dev or CI, where /app is bind-mounted
  # over the image content. Make every docker compose command — explicit
  # builds and implicit ones during up/run alike — skip the baked-in
  # composer and JS asset builds (see the build args in docker-compose.yml).
  SKIP_BAKED_IN_BUILDS: "true"

includes:
  translations: ./task/Taskfile.translations.yml
  lagoon: ./task/Taskfile.lagoon.yml
  database-snapshot: ./task/Taskfile.database-snapshot.yml

vars:
  # Docker image registry.
  # Eg.
  #   - ghcr.io/danskernesdigitalebibliotek
  #   - docker.io/someregistry
  RELEASE_IMAGE_REGISTRY: '{{.RELEASE_IMAGE_REGISTRY | default "ghcr.io/danskernesdigitalebibliotek"}}'
  # Registry host used for docker login (must not include namespace/path).
  RELEASE_IMAGE_REGISTRY_HOST: '{{ index (splitList "/" .RELEASE_IMAGE_REGISTRY) 0 }}'
  # Get total amount of commits on the main branch. Used as build number.
  COMMIT_COUNT:
    sh: git rev-list --count origin/main
  # The version number we want to tag the source build with.
  # It can be specified by adding RELEASE_TAG=XX when running command.
  # Otherwise it will default to the COMMIT_COUNT variable.
  RELEASE_IMAGE_TAG: "{{.RELEASE_IMAGE_TAG | default .COMMIT_COUNT }}"
  # Constructing docker image name.
  DOCKER_IMAGE_PREFIX: "{{ .RELEASE_IMAGE_REGISTRY }}/{{ .DOCKER_IMAGE_NAMESPACE }}"
  # Constructing docker image name.
  RELEASE_IMAGE_NAME: '{{.RELEASE_IMAGE_NAME | default "dpl-cms-source"}}'
  RELEASE_FULL_NAME: "{{.RELEASE_IMAGE_REGISTRY}}/{{.RELEASE_IMAGE_NAME}}:{{.RELEASE_IMAGE_TAG}}"
  DB_SNAPSHOT_IMAGE_NAME: '{{.DB_SNAPSHOT_IMAGE_NAME | default "dpl-cms-database-snapshot"}}'
  DB_SNAPSHOT_FULL_NAME: "{{.RELEASE_IMAGE_REGISTRY}}/{{.DB_SNAPSHOT_IMAGE_NAME}}:{{.RELEASE_IMAGE_TAG}}"
  DB_SNAPSHOT_LATEST_NAME: "{{.RELEASE_IMAGE_REGISTRY}}/{{.DB_SNAPSHOT_IMAGE_NAME}}:latest-default"

  # Where is the docker file(s) we use for our builds residing?
  LAGOON_DIR: "lagoon"

  DOCKER_COMPOSE_FILES_BNF:
    sh: if [[ -f web/sites/sites.php ]]; then echo "-f docker-compose.bnf.yml"; fi
  DOCKER_COMPOSE_FILES_DEFAULT: "-f docker-compose.yml {{ .DOCKER_COMPOSE_FILES_BNF}}"
  DOCKER_COMPOSE_FILES: "{{.DOCKER_COMPOSE_FILES | default .DOCKER_COMPOSE_FILES_DEFAULT }}"
  DOCKER_COMPOSE_FILES_CI: "{{.DOCKER_COMPOSE_FILES}} -f docker-compose.ci.yml"

  # Sql dump files directory
  DIR_RESTORE_DATABASE: "restore/database"
  # Directory with lagoon backup files.
  DIR_RESTORE_FILES: "restore/files"

  # Default language
  LANGUAGE: "da"

  # Ports and host names:
  # Using ports exposed on localhost is important.
  # Host names or ports exposed by Dory or other similar projects
  # introduce multiple layers of proxies and cause problems.
  #
  # Cypress.
  CYPRESS_BASE_PORT:
    sh: docker compose port varnish 8080 2>/dev/null | cut -d ":" -f2
  CYPRESS_HOST_LOCAL: "http://localhost:{{ .CYPRESS_BASE_PORT }}"
  CYPRESS_WIREMOCK_PORT:
    sh: docker compose port wiremock 80 2>/dev/null | cut -d ":" -f2
  CYPRESS_WIREMOCK_LOCAL: "http://localhost:{{ .CYPRESS_WIREMOCK_PORT }}"

tasks:
  ghcr:login:
    summary: Login into Github Container Registry
    cmds:
      - echo {{ .CR_PAT }} | docker login {{ .RELEASE_IMAGE_REGISTRY_HOST }} -u "${GITHUB_ACTOR:-github-actions[bot]}" --password-stdin
    preconditions:
      - sh: "[ ! -z {{.CR_PAT}} ]"
        msg: "Env variable CR_PAT is not set or empty."

  dev:watch:
    summary: Watch source for changes and clear cache.
    watch: true
    sources:
      - 'web/modules/**/*'
      - 'web/themes/**/*'
    cmds:
      - task dev:cli -- drush cache-rebuild

  dev:watch:bnf:
    summary: Watch source for changes and clear cache, including BNF.
    watch: true
    sources:
      - 'web/modules/**/*'
      - 'web/themes/**/*'
    cmds:
      - task dev:cli -- drush cache-rebuild
      - task dev:bnfcli -- drush cache-rebuild

  dev:bnf:enable:
    summary: Set env up for BNF development.
    silent: true
    cmds:
      - cp assets/sites.php web/sites
      - echo 'Enabled, remember to run "task dev:reset"'

  dev:bnf:disable:
    summary: Reset env to non-BNF development.
    silent: true
    cmds:
      - rm web/sites/sites.php
      - echo 'Disabled, remember to run "task dev:reset"'

  dev:format:
    summary: Lint custom code, and fix when possible
    cmds:
      - cmd: task dev:cli -- php vendor/bin/phpcbf
        # PHP Code Beautifier will fail even if it has fixed all errors.
        # Continue regardless assumming that PHP Codesniffer will fail on
        # any remaining errors.
        ignore_error: true
      - task dev:cli -- php vendor/bin/phpcs
      - task dev:cli -- php vendor/bin/twig-cs-fixer lint --fix
      - task dev:cli -- php vendor/bin/phpstan
      - task dev:cli -- corepack pnpm install --frozen-lockfile
      - task dev:cli -- corepack pnpm exec eslint web -c eslint.config.mjs --fix
      - task dev:cli -- corepack pnpm exec eslint cypress -c cypress/eslint.config.mjs --fix

  dev:build:
    summary: Build docker containers
    # Ensure that we only rebuild if there are changes to Dockerfiles
    # or the config files used.
    sources:
      - "{{ .LAGOON_DIR }}/**/*"
      - "{{ .LAGOON_DIR }}/*"
      - "web/sites/sites.php"
      # The compose files define build args and the ignore file defines the
      # build context, so both change what a build produces.
      - "docker-compose*.yml"
      - "../.dockerignore"
    cmds:
      - docker compose {{ .DOCKER_COMPOSE_FILES }} build

  dev:cli:
    summary: Performs command inside container. Expects parameter(s).
    # This could have a dependency on dev:build but does not to avoid excessive
    # log output. dev:cli gets called a lot.
    cmds:
      - docker compose {{ .DOCKER_COMPOSE_FILES }} run --rm cli sh -c "{{.CLI_ARGS}}"

  dev:bnfcli:
    summary: dev:cli for BNF site, a noop if BNF not enabled
    status:
      - "[[ -z \"{{ .DOCKER_COMPOSE_FILES_BNF}}\" ]]"
    cmds:
      - docker compose {{ .DOCKER_COMPOSE_FILES }} run --rm bnfcli sh -c "{{.CLI_ARGS}}"

  dev:start:
    summary: Run docker compose
    deps:
      - dev:build
    cmds:
      - docker compose {{ .DOCKER_COMPOSE_FILES }} up -d {{if eq .CI "true"}}--quiet-pull{{end}}
    vars:
      CI:
        sh: '[[ -z "${CI}" ]] && echo "false" || echo "true"'

  dev:stop:
    summary: Stop docker compose environment
    cmds:
      - docker compose {{ .DOCKER_COMPOSE_FILES }} stop

  dev:down:
    summary: Stop and remove docker compose environment
    cmds:
      # DNS proxies such as Dory may hang on to the network for a bit which
      # causes the process to fail. Wait and retry if that is the case
      - docker compose {{ .DOCKER_COMPOSE_FILES }} down --volumes --remove-orphans || (sleep 3 && docker-compose down)

  dev:pull:
    summary: Pull latest docker images.
    cmds:
      - docker compose {{ .DOCKER_COMPOSE_FILES }} pull

  dev:reset:
    desc: Create local development setup in a clean state
    cmds:
      - task: dev:reset:clean
        vars:
          MARIADB_IMAGE: "{{ .DB_SNAPSHOT_LATEST_NAME }}"
          SKIP_SITE_INSTALL: "true"
    preconditions:
      - sh: '[[ -z "{{ .DOCKER_COMPOSE_FILES_BNF }}" ]]'
        msg: "Snapshot-based reset currently supports the default CMS only. Use 'task dev:reset:clean' for BNF environments."

  dev:reset:clean:
    desc: Create local development setup in a clean state
    deps:
      # Build new containers if necessary.
      - dev:build
      # Stop potential running environment.
      - dev:down
    cmds:
      # cms/.env is a symlink to the central root .env., then ensure the symlink.
      - '[ -e .env ] || ln -s ../.env .env'
      # Pull the images (necessary for the first reset)
      - '{{ if .MARIADB_IMAGE }}MARIADB_IMAGE={{ .MARIADB_IMAGE }} {{ end }}task dev:pull'
      # Start local environment.
      - '{{ if .MARIADB_IMAGE }}MARIADB_IMAGE={{ .MARIADB_IMAGE }} {{ end }}task dev:start'
      # Build site.
      - task dev:cli -- composer install
      # (Re)run Drupal scaffolding.
      - task dev:cli -- composer drupal:scaffold
      # Build the design-system + React from source and copy them into the CMS
      # (theme assets + libraries).
      - task -d {{.TASKFILE_DIR}}/.. dev:cms:link:design-system dev:cms:link:react
      - '{{ if ne .SKIP_SITE_INSTALL "true" }}task dev:cli -- ./dev-scripts/install-site.sh --install-only{{ end }}'
      - task dev:cli -- ./dev-scripts/install-site.sh --post-install-only {{ if .SKIP_LANGUAGE_IMPORT }}--skip-language-import{{ end }}
      # Possibly install the BNF site.
      - task dev:bnfcli -- ./dev-scripts/install-site.sh --bnf-content --install-only
      - task dev:bnfcli -- ./dev-scripts/install-site.sh --bnf-content --post-install-only {{ if .SKIP_LANGUAGE_IMPORT }}--skip-language-import{{ end }}
      - task dev:bnfcli -- drush --yes config:set system.site name BNF
      # Show a one-time login to the local site(s).
      - task dev:cli -- drush user-login
      - task dev:bnfcli -- drush user-login

  dev:run-cron:
    desc: Run cron in the container
    cmds:
      - task dev:cli -- drush cron

  dev:enable-dev-tools:
    desc: Enable dev modules and settings, which are not to be used in Prod. They are config-ignored
    cmds:
      # If changing this, keep the corresponding line in ./dev-scripts/install-site.sh in sync.
      - task dev:cli -- drush install -y devel dpl_example_content field_ui purge_ui restui uuid_url views_ui dblog

  dev:enable-xdebug:
    desc: "Enable xdebug within the container."
    cmds:
      - XDEBUG_ENABLE=true task dev:start
      - read -p "Press enter to disable Xdebug"
      - task dev:start

  dev:phpunit:
    desc: Run PHPUnit tests with code coverage
    cmds:
      - docker compose run -e XDEBUG_ENABLE=true -e XDEBUG_MODE=coverage,debug cli vendor/bin/phpunit --coverage-text

  dev:create-users:
    desc: Create test users, with test roles
    # If changing this, keep the corresponding lines in ./dev-scripts/install-site.sh in sync.
    cmds:
      - task dev:cli -- drush user:create editor --password="test"
      - task dev:cli -- drush user:role:add 'editor' editor

      - task dev:cli -- drush user:create administrator --password="test"
      - task dev:cli -- drush user:role:add 'administrator' administrator

      - task dev:cli -- drush user:create mediator --password="test"
      - task dev:cli -- drush user:role:add 'mediator' mediator

      - task dev:cli -- drush user:create local_administrator --password="test"
      - task dev:cli -- drush user:role:add 'local_administrator' local_administrator

      - task dev:cli -- drush user:create external_system --password="external_system"
      - task dev:cli -- drush user:role:add 'external_system' external_system

      - task dev:cli -- drush user:create patron --password="test"
      - task dev:cli -- drush user:role:add 'patron' patron

  dev:import-profile-translations:
    desc: Import the latest translations from the translation server.
    # Mirrors what install-site.sh and the post-deploy hooks in .lagoon.yml do.
    # Both the UI and the configuration translations are served from GitHub
    # Pages, not from the repository - see docs/cms/translation.md.
    cmds:
      - task dev:cli -- drush locale-check
      - task dev:cli -- drush locale-update
      - task dev:cli -- drush dpl_po:import-remote-config-po da https://danskernesdigitalebibliotek.github.io/dpl-web/cms/translations/da.config.po

  dev:restore:database:
    desc: 'Restore database from db dump file. Only one sql should be present the "{{ .DIR_RESTORE_DATABASE }}" directory.'
    cmds:
      - task dev:cli -- drush sql:drop -y
      - docker compose exec -T {{ .MYSQL_CONTAINER }} mysql < {{ .SQL_FILE }}
      - task dev:cli -- drush deploy
    preconditions:
      - sh: "[ {{ .SQL_FILES_COUNT }} -gt 0 ]"
        msg: "There are no sql files in {{ .DIR_RESTORE_DATABASE }}/. Cannot continue."
      - sh: "[ {{ .SQL_FILES_COUNT }} -eq 1 ]"
        msg: "There are {{ .SQL_FILES_COUNT }} valid files in {{ .DIR_RESTORE_DATABASE }}/:\n{{ .DIR_RESTORE_DATABASE_CONTENT }}\n...there should be just one."
    vars:
      SQL_FILES_COUNT:
        sh: ls {{ .DIR_RESTORE_DATABASE }}/*.sql | wc -l 2> /dev/null | xargs
      SQL_FILE:
        sh: ls -t "{{ .DIR_RESTORE_DATABASE }}"/*.sql
      DIR_RESTORE_DATABASE_CONTENT:
        sh: ls {{ .DIR_RESTORE_DATABASE }}
      MYSQL_CONTAINER: "mariadb"

  dev:restore:files:
    desc: "Restore files by overwriting existing with the ones from the Lagoon backup package"
    cmds:
      - docker compose exec cli sh cms/dev-scripts/cli-restore-lagoon-files.sh {{ .DIR_RESTORE_FILES }}
      - task dev:cache:clear:all

  dev:cache:clear:all:
    summary: Clears all cache
    cmds:
      - task dev:cache:clear:drupal
      - task dev:cache:clear:external

  dev:cache:clear:drupal:
    summary: Runs Drupal cache rebuild
    cmds:
      - task dev:cli -- drush cache:rebuild -y

  dev:cache:clear:external:
    summary: Purges the varnish cache
    cmds:
      - task dev:cli -- drush cache:rebuild-external -y

  dev:codegen:fbs:
    desc: Generate FBS client package from OpenAPI specification
    cmds:
      - cmd: |
          docker run --rm --user $(id -u):$(id -g) \
          -v ${PWD}/..:/local \
          openapitools/openapi-generator-cli:v7.11.0 generate \
          -i /local/schemas/openapi/fbs-adapter.yaml \
          -g php -o /local/cms/packages/fbs-client \
          -c /local/cms/.openapi-generator/fbs.config.yaml \
          --skip-validate-spec
      # The generator still emits a few implicitly nullable parameters that are
      # deprecated as of PHP 8.4. Rewrite them to explicit `?Type`. See
      # rector-generated.php.
      - task dev:cli -- php vendor/bin/rector process packages/fbs-client --config=rector-generated.php --no-diffs

  dev:codegen:dpl-cms:
    desc: Generate CMS API package from OpenAPI specification
    cmds:
      - cmd: |
          cd packages/cms-api && find . -mindepth 1 ! -name '.openapi-generator-ignore' -exec rm -rf {} +
      - cmd: |
          docker run --rm --user $(id -u):$(id -g) \
          -v ${PWD}:/local \
          openapitools/openapi-generator-cli:v7.11.0 generate \
          -i ./local/openapi.json \
          -g php-symfony -o /local/packages/cms-api \
          -c /local/.openapi-generator/dpl-cms.config.yaml
      # The generator still emits a few implicitly nullable parameters that are
      # deprecated as of PHP 8.4 (e.g. Service/TypeMismatchException.php).
      # Rewrite them to explicit `?Type`. See rector-generated.php.
      - task dev:cli -- php vendor/bin/rector process packages/cms-api --config=rector-generated.php --no-diffs

  dev:codegen:bnf-graphql:
    desc: Generate graphQL client classes. See web/modules/custom/bnf/queries/README.md
    cmds:
      - task dev:bnfcli -- drush sailor:introspect bnf
      - task dev:bnfcli -- drush sailor:codegen bnf
      - task dev:bnfcli -- node dev-scripts/stripGraphqlDescriptions.js dpl-cms.bnf.graphql
      # sailor:introspect doesn't emit fields in a deterministic order
      # — Drupal's schema-build order varies with module load and cache
      # state, so two clean introspections can produce semantically
      # identical SDL with different field ordering. Normalize via the
      # `graphql` package so the committed file is stable regardless of
      # which environment ran the introspection.
      - task dev:bnfcli -- node dev-scripts/normalizeGraphqlSchema.js dpl-cms.bnf.graphql

  dev:codegen:fbi-graphql:
    desc: Generate dpl_fbi graphQL classes (reads /schemas/graphql/dbc-fbi.fbcms-go.graphql).
    cmds:
      - task dev:cli -- drush sailor:codegen dpl_fbi

  dev:codegen:proxy-classes:
    desc: Regenerate Drupal lazy-service proxy classes for custom modules.
    # Add an entry per service marked `lazy: true` in any custom module's
    # *.services.yml: "<fully qualified class>|<namespace root relative to
    # web/>". CheckProxyClasses verifies the result matches what is committed.
    cmds:
      - for:
          - 'Drupal\dpl_go\GoSite|modules/custom/dpl_go/src'
        cmd: |
          docker compose {{ .DOCKER_COMPOSE_FILES }} run --rm --workdir /app/cms/web cli php core/scripts/generate-proxy-class.php '{{ splitList "|" .ITEM | first }}' {{ splitList "|" .ITEM | last }}

  dev:example-content:update:
    desc: Update the example content module with data from the current site
    cmds:
      - cmd: task dev:cli -- drush default-content:export-module dpl_example_content

  dev:go:set-graphql-secret:
    desc: Sets the GraphQL consumer secret based on the .env variable.
    cmds:
      - cmd: task dev:cli -- drush dpl_consumers:set-consumer-secret

  dev:go:graphql-credentials:
    desc: Get the GraphQL consumer credentials
    cmds:
      - cmd: task dev:cli -- drush dpl_consumers:consumer-credentials

  ci:reset:
    desc: Create CI setup
    cmds:
      # CI has no 1Password access, so the central .env cannot be generated the
      # usual way. dev:reset only symlinks cms/.env to it and would leave a
      # dangling link.
      - task -d {{.TASKFILE_DIR}}/.. dev:dotenv:template
      - task dev:reset
    env:
      DOCKER_COMPOSE_FILES: "{{ .DOCKER_COMPOSE_FILES_CI }}"
      SKIP_LANGUAGE_IMPORT: "true"

  ci:reset:clean:
    desc: Create CI setup with a clean database
    cmds:
      - task -d {{.TASKFILE_DIR}}/.. dev:dotenv:template
      - task dev:reset:clean
    env:
      DOCKER_COMPOSE_FILES: "{{ .DOCKER_COMPOSE_FILES_CI }}"
      SKIP_LANGUAGE_IMPORT: "true"

  ci:local:
    desc: Make CI setup usable from a local browser.
    cmds:
      # These should be in sync with x-links in docker-compose.ci.yml.
      - ./dev-scripts/add-to-etc-hosts.sh wiremock fbi.dpl-cms.local
      - ./dev-scripts/add-to-etc-hosts.sh wiremock fbs.dpl-cms.local
      - ./dev-scripts/add-to-etc-hosts.sh wiremock adgangsplatformen.dpl-cms.local
      - ./dev-scripts/add-to-etc-hosts.sh wiremock responder.wt-safetag.com
    env:
      DOCKER_COMPOSE_FILES: "{{ .DOCKER_COMPOSE_FILES_CI }}"

  ci:cypress:
    desc: Run Cypress functional tests
    deps:
      - ci:cypress:mapped
      - ci:cypress:unmapped
      - ci:cypress:webmaster
      - ci:cypress:opening-hours
      - ci:cypress:paragraphs
      - ci:cypress:campaigns

  ci:cypress:run:
    cmds:
      - task dev:cli -- drush user:password $CYPRESS_DRUPAL_USERNAME $CYPRESS_DRUPAL_PASSWORD
      - task dev:cli -- drush pm:enable -y dpl_webmaster
      - docker compose {{ .DOCKER_COMPOSE_FILES_CI }} run --rm -e CYPRESS_DRUPAL_USERNAME=$CYPRESS_DRUPAL_USERNAME -e CYPRESS_DRUPAL_PASSWORD=$CYPRESS_DRUPAL_PASSWORD cypress --spec "$SPEC_PATH"
    env:
      DOCKER_COMPOSE_FILES: "{{ .DOCKER_COMPOSE_FILES_CI }}"
      CYPRESS_DRUPAL_USERNAME: admin
      CYPRESS_DRUPAL_PASSWORD: admin
      # To be overriden by the tasks that use this task.
      SPEC_PATH: ""

  ci:cypress:unmapped:
    desc: Run Cypress functional tests
    # Turns out that unmapped tests aren't completely detached from
    # wiremock after all. adgangsplatformen.cy.ts fails without
    # wiremock, but movning it to withMappings makes the other tests
    # fail. So add in the mappings for the moment being.
    deps: [ci:wiremock:create-mappings]
    cmds:
      - task ci:cypress:run
    env:
      SPEC_PATH: "cypress/e2e/*.ts"

  ci:cypress:mapped:
    desc: Run Cypress functional tests
    deps: [ci:wiremock:create-mappings]
    cmds:
      - task ci:cypress:run
    env:
      SPEC_PATH: "cypress/e2e/withMappings/*.ts"

  ci:cypress:webmaster:
    desc: Run Cypress tests for webmaster functionality.
    cmds:
      - task ci:cypress:run
    env:
      SPEC_PATH: "cypress/e2e/webmaster/*.ts"

  # The opening hours tests take an extraordinarly long time. To speed up the
  # GitHub Actions, we've split these out to run in parallel.
  ci:cypress:opening-hours:
    desc: Run Cypress tests for opening hours.
    deps: [ci:wiremock:create-mappings]
    cmds:
      - task ci:cypress:run
    env:
      SPEC_PATH: "cypress/e2e/openingHours/*.ts"

  # The paragraphs tests take an extraordinarly long time. To speed up the
  # GitHub Actions, we've split these out to run in parallel.
  ci:cypress:paragraphs:
    desc: Run Cypress tests for paragraphs.
    deps: [ci:wiremock:create-mappings]
    cmds:
      - task ci:cypress:run
    env:
      SPEC_PATH: "cypress/e2e/paragraphs/*.ts"

  # The campaign tests take an extraordinarly long time. To speed up the
  # GitHub Actions, we've split these out to run in parallel.
  ci:cypress:campaigns:
    desc: Run Cypress tests for campaigns.
    deps: [ci:wiremock:create-mappings]
    cmds:
      - task ci:cypress:run
    env:
      SPEC_PATH: "cypress/e2e/campaigns/*.ts"

  # The events tests take an extraordinarly long time. To speed up the
  # GitHub Actions, we've split these out to run in parallel.
  ci:cypress:events:
    desc: Run Cypress tests for events.
    deps: [ci:wiremock:create-mappings]
    cmds:
      - task ci:cypress:run
    env:
      SPEC_PATH: "cypress/e2e/events/*.ts"

  ci:cypress:local:
    desc: Run Cypress functional tests locally
    deps: [ci:wiremock:create-mappings, ci:local]
    cmds:
      - pnpm install --frozen-lockfile
      - task dev:cli -- drush user:password $CYPRESS_DRUPAL_USERNAME $CYPRESS_DRUPAL_PASSWORD
      - pnpm exec cypress open
    env:
      DOCKER_COMPOSE_FILES: "{{ .DOCKER_COMPOSE_FILES_CI }}"
      CYPRESS_BASE_URL: http://localhost:{{ .CYPRESS_BASE_PORT }}
      CYPRESS_WIREMOCK_URL: "{{ .CYPRESS_WIREMOCK_LOCAL }}"
      CYPRESS_DRUPAL_USERNAME: admin
      CYPRESS_DRUPAL_PASSWORD: admin

  ci:pa11y:
    desc: Run Pa11y to test accessiblity
    deps: [ci:wiremock:create-mappings]
    cmds:
      - docker compose {{ .DOCKER_COMPOSE_FILES_CI }} run --rm node-chrome pnpm --config.verify-deps-before-run=false run pa11y:ci
  ci:wiremock:create-mappings:
    cmds:
      - docker compose {{ .DOCKER_COMPOSE_FILES_CI }} run --rm node-chrome pnpm --config.verify-deps-before-run=false run wiremock:create-mappings

  # Dump the wiremock container's (--verbose) logs. On a mismatch wiremock logs
  # the incoming request plus the closest stub and a diff, which is what we need
  # to see why an FBI GraphQL request returned 404 in CI.
  ci:debug:wiremock-logs:
    desc: Dump wiremock container logs (unmatched-request diffs)
    cmds:
      - docker compose {{ .DOCKER_COMPOSE_FILES_CI }} logs --no-color wiremock

  ci:lighthouse:
    desc: Run Lighthouse to test performance
    deps: [ci:wiremock:create-mappings]
    cmds:
      - docker compose {{ .DOCKER_COMPOSE_FILES_CI }} run --rm node-chrome pnpm dlx lhci autorun

  ci:openapi:validate:
    desc: Validate the Drupal OpenAPI specification
    cmds:
      - docker compose {{ .DOCKER_COMPOSE_FILES_CI }} run --rm node-chrome pnpm dlx swagger-cli validate http://varnish:8080/openapi/rest?_format=json&language=en

  ci:openapi:download:
    desc: Download the Drupal OpenAPI specification from the running local site to the local filesystem
    cmds:
      - task dev:cli -- curl "http://varnish:8080/openapi/rest?_format=json&language=en" -o openapi.tmp
      - task -d .. dev:cli -- sh -c "cd cms && corepack pnpm dlx jsome -r < openapi.tmp > openapi.json"
      - rm -f openapi.tmp

  ci:shellcheck:
    desc: Run ShellCheck on CMS helper scripts
    cmds:
      - find dev-scripts -type f -name '*.sh' -print -exec docker run --rm -v "$(pwd):/mnt" koalaman/shellcheck:stable --format=gcc {} +

  source:build:
    summary: Build core source image.
    cmds:
      - docker build -f {{ .LAGOON_DIR }}/cli.dockerfile --tag dpl-cms-cli:0.0.0 ..
      - docker build -f {{ .LAGOON_DIR }}/source.dockerfile --tag {{ .RELEASE_FULL_NAME }} ..

  source:push:
    summary: Push core source image to container registry.
    deps: [ghcr:login]
    cmds:
      - docker push {{ .RELEASE_FULL_NAME }}

  source:deploy:
    desc: Build and push core source docker image.
    cmds:
      - task: source:build
      - task: source:push
